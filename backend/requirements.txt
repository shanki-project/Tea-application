# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
python-multipart==0.0.20   # form parsing (OAuth2 token form / Swagger Authorize)

# --- Settings / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Auth (JWT + password hashing) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1              # pinned: passlib 1.7.4 is incompatible with bcrypt>=4.1

# --- Database (MySQL) ---
SQLAlchemy==2.0.36
PyMySQL==1.1.1
cryptography==44.0.0       # required by PyMySQL for MySQL 8 caching_sha2_password auth
alembic==1.14.0
